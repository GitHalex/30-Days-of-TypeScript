const mySet: Set<number> = new Set<number>();
console.log(typeof mySet);
mySet.add(1);
console.log(mySet);

const stringSet: Set<string> = new Set<string>();

/* interface Person {
  name: string;
  age: number;
}

const personSet: Set<Person> = new Set<Person>(); */

/* const languages: string[] = [
  "English",
  "Finnish",
  "English",
  "French",
  "Spanish",
  "English",
  "French",
];

// Crear un conjunto a partir del array de lenguajes
const setOfLanguages: Set<string> = new Set<string>(languages);

// Imprimir el conjunto en la consola
console.log(setOfLanguages); */
